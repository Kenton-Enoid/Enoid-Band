from django.apps import AppConfig

class EnoidAppConfig(AppConfig):
    """
    Represents the configuration for the Enoid app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'enoid_app'

